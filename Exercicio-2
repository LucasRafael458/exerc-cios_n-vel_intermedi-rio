#include <stdio.h>

int main()
{
    int n, i;
    float numero, maior, menor, soma = 0, media;

    printf("Quantos numeros deseja digitar? ");
    scanf("%d", &n);

    for (i = 1; i <= n; i++)
    {
        printf("Digite o numero %d: ", i);
        scanf("%f", &numero);

        if (i == 1)
        {
            maior = numero;
            menor = numero;
        }
        else
        {
            if (numero > maior)
            {
                maior = numero;
            }

            if (numero < menor)
            {
                menor = numero;
            }
        }

        soma = soma + numero;
    }

    media = soma / n;

    printf("Maior: %.2f\n", maior);
    printf("Menor: %.2f\n", menor);
    printf("Media: %.2f\n", media);

    return 0;
}
